{{outlet}}

{{notification-container notifications=notifications}}

{{#bs-modal title=modal.modalTitle open=viewModal footer=false}}
	{{#if modal.modalTemplete}}
	  {{component  modal.modalTemplete modal=modal modalclose=(action "removeModal") showNotification=(action "showNotification")}}
	{{/if}}
{{/bs-modal}}